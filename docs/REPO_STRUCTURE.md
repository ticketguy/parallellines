# ParallelLines Repo Structure

```text
engine/runtime      packet language + bus
engine/organs       probability, conviction, echo, memory, shadow, fracture
engine/cores        Memory Fabric, Submind, IntuOne
engine/brain        fixed brain lineage, cortex routing, special tokens
engine/training     Little Fig curricula
memory/embers       Ember's Diaries integration
specs               machine specs
docs                design docs
```

Brain lineage rule:

```text
base_model + Little Fig curriculum + Ember memory tokens + tri-core training = ParallelLines brain lineage
```
